# RSA private key (PKCS#1) for signing test JWTs — local dev only
# Format: -----BEGIN RSA PRIVATE KEY-----
JWT_PRIVATE_KEY=""

# User ID to embed in the sub claim of generated test tokens
TEST_USER_ID=test-user-1
